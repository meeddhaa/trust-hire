# Setup notes

## Firebase

Project: **trusthire-bdapps** — https://console.firebase.google.com/project/trusthire-bdapps

Already done (via CLI):
- Project created, Android app registered (`lib/firebase_options.dart`,
  `android/app/google-services.json` generated by `flutterfire configure`)
- Firestore database created (`nam5`) with baseline security rules deployed
  (`firestore.rules` — see comments there for the server-writes-only design)

**Storage: deliberately not enabled.** Firebase now requires the Blaze
(pay-as-you-go) plan to use Storage at all, which means a billing card on
file. We decided against that for a feature the brief only asks for
conditionally — see "Decision: no Firebase Storage, no resume upload (for
now)" in `docs/ARCHITECTURE.md`. `storage.rules` is written and stays in
the repo unused, in case this gets revisited later.

Auth providers (Email/Password, Google) are enabled — confirmed both via
the console and via `firebase auth:export` from the CLI.

**Debug SHA-1 fingerprint registered** — needed for Google Sign-In on
Android (without it, sign-in fails with `DEVELOPER_ERROR` even though
everything else is configured). This machine's `~/.android/debug.keystore`
fingerprint:
```
5C:A1:4D:53:24:4D:BF:FF:9A:32:14:35:67:E9:A9:12:AB:6C:29:4F
```
is registered under Project Settings → Your apps → trusthire (Android).
Confirmed live: the re-pulled `google-services.json` now carries a real
`oauth_client`/`certificate_hash` entry instead of an empty array.

**Still needed eventually:** a release build (for judging) will need its
own release-keystore SHA-1 added here too, once that keystore exists —
regenerate any machine's fingerprint with `cd android && ./gradlew signingReport`.

## Fonts

Fraunces and Manrope (both OFL-licensed) are bundled as variable-font assets
under `assets/fonts/` rather than fetched via `google_fonts` at runtime, so
the app never depends on a network call just to render text — relevant for
a judged demo on unpredictable venue wifi. License: `assets/fonts/OFL.txt`.

## GitHub

No `gh` CLI is available in this environment. Once you're ready to push:
```
gh repo create trusthire --private --source=. --remote=origin
git push -u origin main
```
or create the repo manually on GitHub and `git remote add origin <url>`.
